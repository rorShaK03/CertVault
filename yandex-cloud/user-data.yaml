#cloud-config
datasource:
Ec2:
  strict_id: false
ssh_pwauth: no
users:
  - name: {{ env.YC_VM_USERNAME }}
    groups: sudo
    shell: /bin/bash
    sudo: [ 'ALL=(ALL) NOPASSWD:ALL' ]
    ssh-authorized-keys:
      - {{ env.YC_VM_SSH }}